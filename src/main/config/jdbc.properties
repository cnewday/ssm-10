jdbc.driverClass=com.mysql.jdbc.Driver
jdbc.user=root
jdbc.password=lt2012ABC

#The indication of whether objects will be validated before being borrowed from the pool
jdbcPool.testOnBorrow=true

#The indication of whether objects will be validated by the idle object evictor (if any)
jdbcPool.testWhileIdle=true

#The SQL query that will be used to validate connections from this pool before returning them to the caller
jdbcPool.validationQuery="SELECT 1"

#The number of milliseconds to sleep between runs of the idle connection validation/cleaner thread
jdbcPool.timeBetweenEvictionRunsMillis=600000


##########\u6B63\u5F0F\u73AF\u5883###################

#jdbc.url=jdbc:mysql://localhost:3306/phone_store?useUnicode=true&characterEncoding=utf-8

##########\u6D4B\u8BD5\u73AF\u5883###################

jdbc.url=jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf-8

####################Hibernate#########################
db.hibernate.dialect=org.hibernate.dialect.MySQLDialect
db.hibernate.max_fetch_depth=3
db.hibernate.jdbc.fetch_size=50
db.hibernate.jdbc.batch_size=50
db.hibernate.show_sql=true



